-- STAR WOMEN cards (category='woman') from Wikidata. Idempotent:
-- each row inserts only when no card with that name exists yet.
-- 0 new of 30 requested (28 already in deck, 2 unresolved).

NOTIFY pgrst, 'reload schema';
