requests>=2.31.0
python-dotenv>=1.0.0
pytest>=8.0.0
hypothesis>=6.100.0
mutmut>=2.4.0
# ⚠️ NUMPY НУЖЕН CI, А НЕ ТОЛЬКО ОПЫТУ. На нём считается обучение прогнозистов
# (forecast_duel.py) и его тесты. Локально он стоял в системе, и прогон был
# зелёным; CI ставит ровно этот файл — и упал с ModuleNotFoundError на сборе
# тестов, уронив ВЕСЬ python-job, а не один файл. Разница «работает у меня» и
# «работает в CI» здесь ровно в одной строке.
numpy>=1.26.0
